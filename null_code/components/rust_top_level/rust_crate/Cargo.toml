[package]
name = "rust_crate"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[toolchain]
channel = "esp"

[dependencies]
pnet_base = "0.35.0"
esp-idf-sys = {version = "0.34", features = ["binstart"]}

#[build-dependencies]
#bindgen = "0.71.0"
